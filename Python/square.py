from functions import square
#import functions //this brings in the whole library

for i in range(10):
    print(f"the number {i} squared is {square(i)}")
    #print(f"the number {i} squared is {function.square(i)}")